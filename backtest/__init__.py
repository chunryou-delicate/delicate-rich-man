"""백테스트 패키지 — 로드맵 2단계.

스크리닝 규칙(현재: 저PBR+저PER 순위합)을 과거 데이터로 시뮬레이션해
"이대로 샀다 팔았으면?"을 검증한다. KRX 일자별 지표(pykrx)만 사용 —
재무(DART) 팩터는 point-in-time 수집을 붙인 뒤(나중) 추가.

⚠️ 백테스트 함정(로드맵 2단계): 과최적화·look-ahead·생존편향·거래비용.
이 패키지는 각각을 명시적으로 방어한다(주석 참조).
"""
